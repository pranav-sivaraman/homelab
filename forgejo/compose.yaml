name: forgejo
networks:
  caddy:
    external: true
services:
  server:
    container_name: forgejo
    environment:
      - USER_UID=568
      - USER_GID=568
      - DOMAIN=https://git.praarthana.space
    image: codeberg.org/forgejo/forgejo:11
    networks:
      - caddy
    restart: always
    volumes:
      - /mnt/home/apps/forgejo/data:/data
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
